package com.example.todo.util;

import lombok.experimental.UtilityClass;

/**
 * Utility class containing the validation functionalities.
 */
@UtilityClass
public class ValidationUtil {

    /**
     * Method that validates if the given "email" is valid (or not)
     * @param email email
     * @return flag whether the email is valid (or not)
     */
    public boolean isValidEmail(final String email) {
        return Boolean.TRUE;
    }
}
